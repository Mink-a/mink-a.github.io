---
interface Props {
  label: string;
}
const { label } = Astro.props;
---
<span class="inline-flex items-center px-3 py-1 text-xs lg:px-4 lg:py-1.5 lg:text-sm rounded-full bg-accent-soft text-accent border border-transparent">
  {label}
</span>
