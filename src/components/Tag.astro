---
interface Props {
  label: string;
}
const { label } = Astro.props;
---
<span class="inline-flex items-center px-4 py-1.5 text-sm rounded-full bg-accent-soft text-accent border border-transparent">
  {label}
</span>
